<ul class="nav">
  <li>
    <%= link_to "Postit!", root_path, :class => 'brand' %>
  </li>
  <% if logged_in? %>
    <li>
      <%= link_to 'New Post', new_post_path %>
    </li>
  <% end %>
  <li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Category <span class="caret"></span></a>
    <ul class="dropdown-menu" role="menu">
      <% Category.all.each do |category| %>
      <li><%= link_to category.name, category_path(category) %></a></li>
      <% end %>
      <% if logged_in? %>
        <li class='divider'></li>
        <li><%= link_to 'New Category', new_category_path%></li>
      <% end %>
    </ul>
  </li>

  <% if !logged_in? %>
    <li><button type='button' class='btn btn-success navbar-btn'><%= link_to "Register", '/register' %></button></li>
    <button type='button' class='btn btn-default navbar-btn'><%= link_to "Log In", '/login' %></button>
  <% else %>
    <button type='button' class='btn btn-default navbar-btn'><%= link_to "Log Out", '/logout' %></button>
  <% end %>
</ul>
